class CLook:
    """"
    Receive a lot and process it, return the lot 
    like Scan algorithm says
    """
    def resolve(self, lot = []):
        return lot



class CScan:
    """"
    Receive a lot and process it, return the lot 
    like Scan algorithm says
    """
    def resolve(self, lot = []):
        return lot



class Fcfs:
    """
    Receive a lot and process it, return the lot 
    like Scan algorithm says
    """
    def resolve(self, lot = []):
        return lot



class Fifo:
    """
    Receive a lot and process it, return the lot 
    like Scan algorithm says
    """
    def resolve(self, lot = []):
        return lot



class Look:
    """
    Receive a lot and process it, return the lot 
    like Scan algorithm says
    """
    def resolve(self, lot = []):
        return lot



class Scan:
    """"
    Receive a lot and process it, return the lot 
    like Scan algorithm says
    """
    def resolve(self, lot = []):
        return lot
